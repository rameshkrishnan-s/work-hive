<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <style>
        /* Basic reset and styling */
        body, html {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #f9f9f9;
            text-align: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }

        /* Container for the content */
        .container {
            animation: fadeIn 2s ease-out;
        }

        /* Fade-in animation for content */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Button styles */
        a {
            text-decoration: none;
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            margin: 10px;
            display: inline-block;
            transition: background-color 0.3s ease;
        }

        a:hover {
            background-color: #0056b3;
        }

        /* Image styling */
        .hero-img {
            width: 60%;
            max-width: 400px;
            margin-bottom: 20px;
            animation: slideIn 2s ease-out;
        }

        /* Slide-in animation for image */
        @keyframes slideIn {
            from { opacity: 0; transform: translateY(-50px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Heading animation */
        h1 {
            color: #333;
            font-size: 2rem;
            animation: popIn 1s ease-out;
        }

        @keyframes popIn {
            from { transform: scale(0.8); opacity: 0; }
            to { transform: scale(1); opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Image for visual appeal -->
        <img src="https://th.bing.com/th/id/OIP.VDuc2MpsfWlvFpFi1OEYGwHaE8?w=306&h=204&c=8&rs=1&qlt=90&o=6&dpr=1.3rm=2&w=1200&h=800&dpr=1.3&pid=ImgDetMain" alt="Job Portal" class="hero-img">
        
        <% if (message) { %>
            <h1><%= message %></h1>
        <% } else { %>
            <h1>Welcome to the Job Portal</h1>
        <% } %>

        <!-- Navigation links -->
        <div>
            <a href="/login">Login</a> | <a href="/register">Register</a>
        </div>
    </div>
</body>
</html>
